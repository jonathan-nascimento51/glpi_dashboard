# =============================================================================
# GLPI Dashboard - Configurações de Ambiente
# =============================================================================
# Este arquivo contém todas as variáveis de ambiente necessárias para o projeto.
# Copie este arquivo para .env e ajuste os valores conforme necessário.

# -----------------------------------------------------------------------------
# AMBIENTE E RELEASE
# -----------------------------------------------------------------------------
ENVIRONMENT=development
RELEASE=1.0.0
FLASK_ENV=development

# -----------------------------------------------------------------------------
# GLPI - Configurações de Integração
# -----------------------------------------------------------------------------
# URL base da API do GLPI
GLPI_URL=https://your-glpi-instance.com/apirest.php

# Token de aplicação do GLPI (obrigatório)
GLPI_APP_TOKEN=your_app_token_here

# Token de usuário do GLPI (obrigatório)
GLPI_USER_TOKEN=your_user_token_here

# Timeout para requisições GLPI (segundos)
GLPI_TIMEOUT=30

# -----------------------------------------------------------------------------
# FEATURE FLAGS
# -----------------------------------------------------------------------------
# Habilita dados mock para desenvolvimento
FLAG_USE_MOCK_DATA=false

# Habilita KPIs da versão 2
FLAG_USE_V2_KPIS=false

# Habilita arquitetura hexagonal
FLAG_USE_HEXAGONAL=false

# -----------------------------------------------------------------------------
# CACHE E PERFORMANCE
# -----------------------------------------------------------------------------
# URL do Redis para cache
REDIS_URL=redis://localhost:6379/0

# Timeout do cache (segundos)
CACHE_TIMEOUT=300

# TTL padrão do cache (segundos)
CACHE_DEFAULT_TTL=3600

# -----------------------------------------------------------------------------
# LOGGING E DEBUGGING
# -----------------------------------------------------------------------------
# Nível de log (DEBUG, INFO, WARNING, ERROR, CRITICAL)
LOG_LEVEL=INFO

# Formato do log (json, text)
LOG_FORMAT=json

# Arquivo de log (opcional)
LOG_FILE=

# -----------------------------------------------------------------------------
# SEGURANÇA E CORS
# -----------------------------------------------------------------------------
# Origens permitidas para CORS (separadas por vírgula)
CORS_ORIGINS=http://localhost:3000,http://localhost:8050

# Habilita middleware de segurança
SECURITY_HEADERS_ENABLED=true

# Content Security Policy - Configurações
CSP_DEFAULT_SRC=\'self\'
CSP_SCRIPT_SRC=\'self\' \'unsafe-inline\'
CSP_STYLE_SRC=\'self\' \'unsafe-inline\'
CSP_IMG_SRC=\'self\' data: https:
CSP_CONNECT_SRC=\'self\'

# -----------------------------------------------------------------------------
# MONITORAMENTO - SENTRY
# -----------------------------------------------------------------------------
# DSN do Sentry para monitoramento de erros
SENTRY_DSN=

# Taxa de amostragem para traces (0.0 a 1.0)
SENTRY_TRACES_SAMPLE_RATE=0.1

# Taxa de amostragem para profiles (0.0 a 1.0)
SENTRY_PROFILES_SAMPLE_RATE=0.1

# -----------------------------------------------------------------------------
# MONITORAMENTO - OPENTELEMETRY
# -----------------------------------------------------------------------------
# Endpoint do coletor OTLP
OTEL_EXPORTER_OTLP_ENDPOINT=

# Nome do serviço para telemetria
OTEL_SERVICE_NAME=glpi-dashboard-api

# -----------------------------------------------------------------------------
# FEATURE FLAGS - UNLEASH
# -----------------------------------------------------------------------------
# URL do servidor Unleash
UNLEASH_URL=

# Nome da aplicação no Unleash
UNLEASH_APP_NAME=glpi-dashboard

# ID da instância no Unleash
UNLEASH_INSTANCE_ID=

# Token de cliente do Unleash
UNLEASH_CLIENT_TOKEN=

# -----------------------------------------------------------------------------
# BANCO DE DADOS (se aplicável)
# -----------------------------------------------------------------------------
# URL de conexão com o banco de dados
DATABASE_URL=

# Pool de conexões - tamanho mínimo
DB_POOL_MIN_SIZE=1

# Pool de conexões - tamanho máximo
DB_POOL_MAX_SIZE=10

# -----------------------------------------------------------------------------
# DESENVOLVIMENTO E TESTES
# -----------------------------------------------------------------------------
# Habilita modo debug
DEBUG=false

# Habilita reload automático
RELOAD=true

# Porta do servidor de desenvolvimento
PORT=8000

# Host do servidor de desenvolvimento
HOST=0.0.0.0

# -----------------------------------------------------------------------------
# SEGREDOS E CHAVES (NUNCA COMMITAR VALORES REAIS)
# -----------------------------------------------------------------------------
# Chave secreta para JWT (gere uma chave forte)
SECRET_KEY=your_secret_key_here_change_in_production

# Chave de criptografia (gere uma chave forte)
ENCRYPTION_KEY=your_encryption_key_here_change_in_production
