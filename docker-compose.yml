version: '3.8'

services:
  redis:
    image: redis:7-alpine
    container_name: glpi_dashboard_redis
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    command: redis-server --appendonly yes --maxmemory 256mb --maxmemory-policy allkeys-lru
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 3s
      retries: 3
      start_period: 10s
    restart: unless-stopped
    networks:
      - glpi_network

  # Opcional: PostgreSQL para dados persistentes (se necessário no futuro)
  # postgres:
  #   image: postgres:15-alpine
  #   container_name: glpi_dashboard_postgres
  #   environment:
  #     POSTGRES_DB: glpi_dashboard
  #     POSTGRES_USER: glpi_user
  #     POSTGRES_PASSWORD: glpi_password
  #   ports:
  #     - "5432:5432"
  #   volumes:
  #     - postgres_data:/var/lib/postgresql/data
  #   healthcheck:
  #     test: ["CMD-SHELL", "pg_isready -U glpi_user -d glpi_dashboard"]
  #     interval: 10s
  #     timeout: 5s
  #     retries: 5
  #   restart: unless-stopped
  #   networks:
  #     - glpi_network

volumes:
  redis_data:
    driver: local
  # postgres_data:
  #   driver: local

networks:
  glpi_network:
    driver: bridge
    name: glpi_dashboard_network